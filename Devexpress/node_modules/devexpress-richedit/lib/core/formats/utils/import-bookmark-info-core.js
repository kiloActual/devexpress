"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var ImportBookmarkInfoCore = (function () {
    function ImportBookmarkInfoCore() {
        this.start = -1;
        this.end = -1;
    }
    ImportBookmarkInfoCore.prototype.validate = function (subDocument) {
        var _a;
        if (this.start < 0 || this.end < 0)
            return false;
        var maxPos = subDocument.getDocumentEndPosition();
        this.start = Math.min(maxPos, this.start);
        this.end = Math.min(maxPos, this.end);
        if (this.start > this.end)
            _a = [this.end, this.start], this.start = _a[0], this.end = _a[1];
        return true;
    };
    return ImportBookmarkInfoCore;
}());
exports.ImportBookmarkInfoCore = ImportBookmarkInfoCore;
